# railway.yml

version: '1.0'
services:
  app:
    env:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    numReplicas: 1
    startCommand: python moneybot.py
    sleepApplication: false
    restartPolicyType: ON_FAILURE
    restartPolicyMaxRetries: 10